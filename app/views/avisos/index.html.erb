<p style="color: green"><%= notice %></p>

<h1>Avisos</h1>

<div id="avisos">
  <% @avisos.each do |aviso| %>
    <%= render aviso %>
    <p>
      <%= link_to "Show this aviso", aviso %>
    </p>
  <% end %>
</div>

<%= paginate @avisos %>

<%= link_to "New aviso", new_aviso_path %>
<p><%= link_to "Home", root_path %></p>
